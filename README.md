# synthvid

A command-line tool that generates video files from a declarative scene
description.

The same description always produces the same bytes, on any platform and any
CPU architecture, and every quantity in the generated frames is reported
exactly in an accompanying manifest.

Early work. Nothing is stable yet.

## Licence

MIT or Apache-2.0, at your option.
